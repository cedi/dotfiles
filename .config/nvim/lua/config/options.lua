-- LazyVim auto-loads this file. Anything here runs after LazyVim's defaults.

-- Allow project-local .nvim.lua / .nvimrc / .exrc files. Neovim still requires
-- explicit :trust per file, so this is safe even on untrusted repos.
vim.opt.exrc = true
